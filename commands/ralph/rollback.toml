prompt = """
Rollback the Ralph Loop state to a previous iteration.

## Args
- count: {{args}}

## Instructions

1. Call the `ralph_rollback_iterations` tool with the count from "{{args}}" (default to 1 if not specified).

2. Report the result and current state to the user.
"""
